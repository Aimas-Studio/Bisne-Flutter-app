class Product {
  int id;
  String name;
  String description;
  num price;
  Product(this.id, this.name, this.description, this.price);
}

class ProductDump {
  String name;
  num price;
  ProductDump(this.name, this.price);
}
